#include <stdio.h>
#define NUMBER 1024


int main()
{
  int i, sum;
  sum=0;

  for (i=0;i<NUMBER;i++)
    {
      if (i % 3 == 0)
	sum+=i;
      else if (i % 5 == 0)
	sum+=i;
      printf("%d ", i);
    }

  printf("\nThe total sum is: %d\n", sum);
  return 0;
}
